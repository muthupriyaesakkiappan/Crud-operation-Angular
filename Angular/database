Create database:
================

create database recipe;

Use Database:
=============
 Use database;

Create Table:
=============
 create table recipe (sno bigint NOT NULL auto_increment,image varchar(255),recipe_name varchar(255) not null, ingredients varchar(255) not null, instructions  varchar(500), primary key(sno));

End points:
===========

- To get all recipe details:

	/getdetails

- To add new recipe :

	/add

- To edit purticular recipe :

	/update/:sno

- To delete recipe r:

	/delete